# ============================================================
# LegionForge — requirements.txt
# Last verified: February 2026 / Python 3.11
# ============================================================

# ── Framework Core ───────────────────────────────────────────
langgraph~=0.2
langchain~=0.3
langchain-core~=0.3
langchain-community~=0.3

# ── LLM Provider Clients ─────────────────────────────────────
langchain-openai~=0.2
langchain-anthropic~=0.3
langchain-ollama~=0.1

# ── Observability ────────────────────────────────────────────
langsmith~=0.1

#Edited by JP per Claude recommendation (20260222)
# ── Persistence — PostgreSQL (replaces SQLite) ───────────────
langgraph-checkpoint-postgres~=2.0
# Commented out due to dependency conflicts per claude (20260222)
# Security: pin SQLite checkpointer to patched version (unused but present)
# Fixes CVE-2025-64104, CVE-2025-64439, CVE-2025-67644
#  langgraph-checkpoint-sqlite>=3.0.1
psycopg[binary,pool]~=3.1
pgvector~=0.3


# ── Config & Validation ──────────────────────────────────────
pydantic~=2.0
pydantic-settings~=2.0
pyyaml~=6.0

# ── Secret Management ────────────────────────────────────────
keyring~=25.0
python-dotenv~=1.0

# ── Token Counting & Rate Limiting ───────────────────────────
tiktoken~=0.7
aiolimiter~=1.1                  # Async rate limiter for API calls

# ── HTTP / Async ─────────────────────────────────────────────
httpx~=0.27
aiohttp~=3.9
tenacity~=8.0                    # Retry logic for API calls
fastapi~=0.111                   # Health/status endpoint
uvicorn~=0.30                    # ASGI server for health endpoint

# ── Dev / Testing ────────────────────────────────────────────
pytest~=8.0
pytest-asyncio~=0.23
